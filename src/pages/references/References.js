import React from "react";

export default class References extends React.Component {
    render() {
        return <div style={{backgroundColor: 'green', width: '100%', height: '100%'}} className={'am-references'}/>
    }
}